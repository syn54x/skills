name: ci

on:
  pull_request:
  push:
    branches: [main]

jobs:
  plugin-skills:
    name: plugin.json lists every skill
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: scripts/check-plugin-skills.sh
